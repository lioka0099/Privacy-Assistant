{
  "name": "@privacy-assistant/shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "dev": "echo \"shared dev not required\"",
    "build": "echo \"shared build setup in step 1.2\"",
    "lint": "eslint . --max-warnings=0",
    "test": "vitest run --pool=threads",
    "typecheck": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "vitest": "^4.0.18"
  }
}